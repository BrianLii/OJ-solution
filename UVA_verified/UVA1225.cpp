/*
    Solution for:
    Problem Link:
    Verdict: Accepted
    Submission ID:
*/

#include <bits/stdc++.h>
using namespace std;

class Solution {
   public:
    void solve() {
        int input;
        for (int pos = 0, current = input; current; pos++, current /= 10) {
            int digit = current % 10;
        }

        for (int pos = 0, current = input; current; current /= 10) {
        }
    }
};

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    Solution().solve();
    return 0;
}